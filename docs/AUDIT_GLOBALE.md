# AUDIT GLOBALE — ERP Bite Digital

> Audit **read-only** consolidato. Nessun codice modificato. Verifiche eseguite chiamando gli endpoint live (`https://localhost`, auth cookie admin) e ispezionando il DB (`bite_erp_db`). Data: 2026-06-02.
> Metodo: 8 subagent in parallelo (aree A–H), stessa rubrica e schema. Fase finale: dedup, ordinamento per gravità, riepilogo.

## Riepilogo per gravità

| Gravità | Conteggio |
|---|---|
| **P0** (rotto in prod / perdita dati / sicurezza) | **4** |
| **P1** (flusso utente rotto o dato sbagliato) | **11** |
| **P2** (UX/perf degradata) | **12** |
| **P3** (cosmetico / cleanup) | **15** |
| **TOTALE** | **42** |

Di cui **ATTESO/noto (non bug reale)**: 6 (A-04, A-07, E-01, E-02, E-03, E-04).

## Riepilogo per area

| Area | Findings | P0 | P1 | P2 | P3 |
|---|---|---|---|---|---|
| A — Auth/Sicurezza/Ruoli | 7 | 0 | 2 | 2 | 3 |
| B — Core CRUD | 6 | 1 | 2 | 1 | 2 |
| C — Studio OS | 6 | 2 | 2 | 1 | 1 |
| D — Team/HR | 4 | 0 | 0 | 1 | 3 |
| E — Finanza (re-check) | 4 | 0 | 0 | 0 | 4 |
| F — Collaboration | 2 | 1 | 1 | 0 | 0 |
| G — BI/Viz | 6 | 0 | 2 | 3 | 1 |
| H — Trasversale | 7 | 0 | 1 | 4 | 2 |

> Nota dedup: **G-01 è la stessa root cause di C-01** (unificati in C-01/G-01); **A-08** confluito in A-01; **G-02** è conseguenza diretta di C-01/G-01. Il conteggio sopra riflette la lista deduplicata.

## TOP 10 P0/P1 da affrontare per primi

| # | ID | Gravità | Titolo | Impatto |
|---|---|---|---|---|
| 1 | B-01 | P0 | `PATCH /clienti/{id}` sempre 500 | Ogni "Modifica cliente" dalla UI fallisce (il dato persiste ma il client riceve 500) |
| 2 | C-01 / G-01 | P0 | Admin/Developer vedono **ZERO task** (scope a set vuoti → `.in_(set())`) | Studio OS (kanban/lista/calendario) e Gantt/Planning vuoti pur con 131 task a DB |
| 3 | C-02 | P0 | Task GET singolo/POST/PATCH sempre 500 (`attachments` lazy-load fuori greenlet) | CRUD task di Studio OS inutilizzabile end-to-end |
| 4 | F-01 | P0 | Modulo Documenti interamente in 500 (colonna `is_private` nel model assente dal DB) | Pagina Documenti morta (tree + create) |
| 5 | A-01 | P1 | IBAN, CF, P.IVA, costo orario risorse leggibili da **qualsiasi** utente autenticato | `GET /risorse` usa `get_current_user` invece di `require_admin` → esposizione dati HR/bancari |
| 6 | B-02 | P1 | Creazione fornitore manuale sempre 400 (`fic_id` NOT NULL) | "Nuovo Fornitore" dalla UI non funziona |
| 7 | C-03 | P1 | Kanban drag&drop: 3 colonne su 4 inviano `stato` non valido → 422 | Spostare card tra colonne non persiste |
| 8 | F-02 | P1 | Chat WebSocket handshake 403 via `https://localhost` (origin non in whitelist) | Chat realtime non si connette in locale/IP (prod dominio OK) |
| 9 | B-03 | P1 | `google_drive_url` del form cliente non persiste mai (drop silenzioso schema) | Link Drive inserito sparisce senza errore |
| 10 | H-01 | P1 | FE chiama `/clickup/progress/{cliente}` ma il router clickup non è montato → 404 | Progress ClickUp in CommessaDetail sempre fallito |

Altri P1 rilevanti subito sotto la top 10: **A-02** (DELETE /piani senza guard ruolo), **C-04** (modal task stato 422), **G-03** (`/planning/tasks` senza `data_inizio`), **A-04** (ruolo PM ancora manager — ATTESO/refactor parcheggiato).

---

## Findings consolidati (ordinati P0 → P3)

| ID | Area | Gravità | Titolo | file:riga | Evidenza (come riprodotto) | Fix suggerito | Rischio del fix | Stato |
|---|---|---|---|---|---|---|---|---|
| B-01 | B | **P0** | `PATCH /clienti/{id}` sempre 500 (ResponseValidationError) | clienti.py:81-92 + services.py:346-356 + audit.py:83 | `curl -X PATCH /clienti/{id} -d '{"note":"x"}'` → 500 (3/3). Doppio `flush()` (update_cliente + emit_update) espira `updated_at` → lazy-load async al serialize → `MissingGreenlet`. Il dato PERSISTE ma il client riceve 500. GET singolo (stesso `ClienteOut`) → 200 | Dopo update+emit ricaricare l'oggetto con get fresco (come patch_progetto/commessa) o `await db.refresh(c)`; rimuovere doppio audit | Basso | BUG REALE |
| C-01 / G-01 | C+G | **P0** | Admin/Developer vedono ZERO task ovunque (scope a set vuoti) | permissions.py:16-18 + services.py:1758,1766-1786 | `get_user_access_scope` per ruolo ERP ritorna `(set(),set(),set())`; in `list_tasks` `if p_ids is not None` (set ≠ None) → `Task.progetto_id.in_(set())` = predicato falso. Live: admin `GET /tasks` → `[]` con 131 task (103 datati) a DB. Riprodotto SQL `WHERE progetto_id = ANY('{}')` → 0 righe | Trattare set vuoti come "accesso pieno": branch esplicito su `has_erp_access` in `list_tasks`, o far ritornare `None`=illimitato e allineare tutti i call-site | Medio (scope usato da più endpoint → preferire fix locale in list_tasks) | BUG REALE |
| C-02 | C | **P0** | Task GET singolo/POST/PATCH → 500 (lazy-load `attachments`) | services.py:1864-1873 (`_get_task_record`) + schemas.py:716 (`TaskOut.attachments`) | `_get_task_record` carica subtasks/assegnatario/revisore/timer ma NON `attachments`; `TaskOut` li include → lazy-load fuori greenlet → `MissingGreenlet`. Live: `POST /tasks`→500, `GET /tasks/{id}`→500, `PATCH /tasks/{id} {"stato":"DA_FARE"}`→500. Task non persistito (rollback) | Aggiungere `selectinload(Task.attachments)` in `_get_task_record` | Basso | BUG REALE |
| F-01 | F | **P0** | Modulo Documenti interamente in 500 — colonna `is_private` nel model assente dal DB | models.py:1135 vs tabella `document_nodes`; documents.py (tutti gli endpoint) | `GET /documents/tree`→500 (`ProgrammingError`), `POST /documents`→500. `SELECT is_private FROM document_nodes` → `column does not exist`. Il model ha `is_private` (mai usato in documents.py) ma schema.sql/DB no. FE `useDocuments.ts:13` → pagina StudioDocumentsPage morta | `ALTER TABLE document_nodes ADD COLUMN is_private BOOLEAN NOT NULL DEFAULT TRUE` + allineare schema.sql; oppure rimuovere il campo inutilizzato dal model | Basso (additivo) | BUG REALE |
| A-01 | A | **P1** | Dati HR/bancari (IBAN, CF, P.IVA, costo orario) leggibili da qualsiasi autenticato (incl. studio-only) | risorse.py:15 (`GET /risorse`), :31 (`GET /risorse/{id}`) — usano `get_current_user` invece di `require_admin` | Token DIPENDENTE: `GET /risorse` → 200; `RisorsaOut` espone `iban, banca, bic_swift, codice_fiscale, piva, indirizzo, costo_orario_override, servizi[].costo_orario`. Lo stesso GET HR (router.py:695) usa `require_admin`. (Include ex-A-08: costi servizi esposti) | `Depends(require_admin)`/`require_finance_access` sulle GET in risorse.py, o schema ridotto per non-admin | Basso (aggiornare i chiamanti studio della lista risorse) | BUG REALE |
| A-02 | A | **P1** | `DELETE /piani/{id}` senza guardia ruolo: studio-only cancella pianificazione che non può leggere | router.py:869-870 (`delete_piano`, solo `get_current_user`) | `GET /piani` (require_finance_access) → 403 per DIPENDENTE, ma `DELETE /piani/<uuid>` → 204 con stesso token. Perdita dati | `Depends(require_finance_access)` come gli altri `/piani` | Basso | BUG REALE |
| B-02 | B | **P1** | Creazione fornitore manuale sempre fallita (NOT NULL su `fic_id`) | services.py:2665-2666 + schemas.py:815 + fornitori.py:99-111 | `curl POST /fornitori -d '{"ragione_sociale":"X"}'` → 400 `NotNullViolation: fic_id`. `create_fornitore` fa `Fornitore(**dump)` ma `FornitoreCreate` non ha `fic_id` (valorizzato solo da sync FiC). FE mostra toast generico | Generare `fic_id` per fornitori manuali (`f"MANUAL-{uuid4().hex}"`) o rendere nullable | Basso | BUG REALE |
| B-03 | B | **P1** | `google_drive_url` del form cliente non persiste mai (drop silenzioso) | schemas.py:229-287 (Create/Update/Out) vs models.py:173; ClienteDialog.tsx:603 | FE invia `google_drive_url` ma gli schema Pydantic non lo dichiarano → scartato. `POST /clienti` 201 ma DB `google_drive_url` = NULL. La colonna model esiste già | Aggiungere `google_drive_url: Optional[str]` a ClienteCreate/Update/Out | Basso | BUG REALE |
| C-03 | C | **P1** | Kanban drag&drop: 3 colonne su 4 inviano `stato` inesistente → 422 | StudioKanbanView.tsx:55-60,326-328 vs models.py:64-71 | COLUMNS usano `IN_CORSO/REVISIONE/COMPLETATO`; enum `TaskStatus` = `DA_FARE/BOZZE_IDEE/DA_CORREGGERE/IN_REVIEW/PRONTO/PROGRAMMATO/PUBBLICATO`. `PATCH /tasks/{id} {"stato":"IN_CORSO"}` → 422. Grouping mappa per id colonna errati → ogni task finisce in "Da Fare" | Allineare COLUMNS+mapping ai valori reali dell'enum (o mappa di traduzione UI↔enum) | Medio (tocca anche modal) | BUG REALE |
| C-04 | C | **P1** | Modal task: dropdown Stato e toggle subtask usano valori `stato` inesistenti → 422 | StudioTaskModal.tsx:456-458 (Select), 226-228 (toggle) | Select offre `IN_CORSO/REVISIONE/COMPLETATO` → 422 al salvataggio; `handleToggleSubtask` invia `stato:"COMPLETATO"` → 422 (oltre al 500 di C-02) | Usare i valori reali dell'enum nei SelectItem e nel toggle | Medio (vedi C-03) | BUG REALE |
| F-02 | F | **P1** | Chat WebSocket handshake 403 via `https://localhost` (origin non in whitelist) | chat.py:435-439 (`_allowed_websocket_origins`) + nginx.conf:14-16 (redirect 80→https) + .env:17,29 | nginx redirige a `https://localhost` → browser invia `Origin: https://localhost` → 403; solo `http://localhost` dà 101. Allowed = FRONTEND_BASE_URL (dominio prod) + CORS (`http://localhost`, non https). Chat realtime non si connette in locale/IP | Aggiungere `https://localhost` (e IP prod https) a `CORS_ORIGINS`, o consentire la variante https di ogni origin | Basso | BUG REALE |
| G-02 | G | **P1** | Vista Gantt in Planning sempre vuota (0 barre) — conseguenza di C-01/G-01 | Planning.tsx:128,300,604 | `ganttTasks=useTasks()` → `/tasks` vuoto (C-01) → `planningGanttTasks` vuoto → `<GanttChart tasks={[]}>` (badge "0 task con date") pur con 103 task datati a DB | Risolto a monte con C-01/G-01; aggiungere empty-state esplicito per distinguere "nessun dato" da "errore" | Basso (dipende da C-01) | BUG REALE (consequenza) |
| G-03 | G | **P1** | `/planning/tasks` non espone `data_inizio` → viste Settimana/Mese e Gantt perdono l'inizio | planning.py:33-44 | Il dict ritorna `data_scadenza` ma non `data_inizio`. `curl /planning/tasks` → 50 righe senza `data_inizio` | Aggiungere `"data_inizio": t.data_inizio.isoformat() if t.data_inizio else None` | Basso (additivo) | BUG REALE |
| H-01 | H | **P1** | FE chiama `/clickup/progress/{cliente}` ma il router clickup non è montato → 404 | CommessaDetail.tsx:90 → routers/clickup.py:144 | La rotta esiste solo in `routers/clickup.py`, mai `include_router` (`grep routers.clickup` = 0). Chiamata FE → 404 | Montare `clickup.router` o rimuovere/gestire fallback FE | Basso (montare) / Medio (capire perché disabilitato) | BUG REALE |
| A-04 | A | **P1** | Ruolo deprecato `PM` (studio-only) trattato come manager in ~30 endpoint commerciali | `require_roles(ADMIN,DEVELOPER,PM)` in clienti.py:34-156, preventivi.py:28-92, pianificazioni.py:41-148, router.py:879..3204, ai.py:479,557 | `is_studio_only_role(PM)==True` MA incluso in require_roles → un PM attivo accederebbe a clienti/preventivi/CRM-stats. Non sfruttabile oggi (tutti i PM `attivo=f`) | Rimuovere `UserRole.PM` dalle `require_roles`; usare `require_erp_access`/`require_admin` | Medio | ATTESO (refactor ruoli parcheggiato su wip/snapshot-2026-06-01; incongruenza presente su main) |
| A-03 | A | **P2** | IDOR: storico timing/produttività di un altro utente leggibile senza ownership check | router.py:1208-1213 (`GET /tasks/time-estimate`, `user_id` query param, solo `get_current_user`) | DIPENDENTE: `GET /tasks/time-estimate?user_id=<altro>&task_type=web` → 200 (media/min/max altrui). `/users/{id}/capacity-today` invece ha check IDOR (403) | Check `user_id == current_user.id or is_manager` come in capacity-today | Basso | BUG REALE |
| A-05 | A | **P2** | CRM e Wiki/Contenuti accessibili a ruoli studio-only | router.py:2805 (`/crm/lead`), 2752 (stadi ok ma lead/attività/converti usano `get_current_user`), wiki/* (2607+), contenuti (1849+) | DIPENDENTE: `GET /crm/lead`/`/wiki/articoli`/`/contenuti` → 200. Il modello dice studio-only = solo Studio OS | Definire policy: se CRM è solo manager, gate con `require_erp_access` | Medio (può rompere Studio OS se contenuti servono) | BUG REALE (da policy) |
| B-04 | B | **P2** | PATCH con `exclude_none` non può azzerare campi a null | fornitori.py:121 + services.py:352 (`model_dump(exclude_none=True)`) | `PATCH /fornitori/{id} -d '{"note":null}'` → null scartato, campo resta valorizzato. Impossibile svuotare un campo dalla UI | Usare `model_dump(exclude_unset=True)` | Medio (verificare null involontari dal FE) | BUG REALE |
| C-05 | C | **P2** | Branch di scope in `list_tasks` parzialmente morto (root di C-01) | services.py:1766-1776 | `get_user_access_scope` non ritorna mai `None` → `if p_ids is not None` sempre True, `elif t_ids is not None` irraggiungibile | Rivedere con C-01: contratto chiaro (None=illimitato) rispettato ovunque | Medio | BUG REALE |
| D-01 | D | **P2** | `GET/POST/PATCH /risorse` DUPLICATI e SHADOWED (inline router.py vs risorse.py) | router.py:695-725 (inline) vs risorse.py:15,47,72 | `risorse.router` incluso (router.py:132) prima dei decorator inline → vince risorse.py. Live: `/risorse` ritorna `servizi` e onora `?includi_inattivi` (= risorse.py); l'inline (con `costo_orario_effettivo`, senza filtro `attivo`) è morto. Trappola: chi si aspetta `costo_orario_effettivo` riceve `undefined` | Rimuovere le definizioni inline (router.py:695-725) + import orfani; risorse.py unica fonte | Basso (inline mai eseguiti) | BUG REALE |
| G-04 | G | **P2** | Date task corrotte nel seed (anno 0464) passate al Gantt | dato DB / seed; GanttChart.tsx (parseISO/differenceInDays) | `/planning/tasks` → `data_scadenza:"0464-06-06"`. `eachDayOfInterval` su start/end espanso a anno 464 → barre/offset enormi; `isValid` non scarta (464 è valida) | Bonificare seed; difensivamente scartare date < anno 2000 in startDate/getGeometry | Basso | BUG REALE |
| G-05 | G | **P2** | `backlogTasks` usa `progettiMap` prima della dichiarazione (TDZ) → crash ricerca backlog | Planning.tsx:197 (uso) vs :283 (const progettiMap) | Il `.filter` legge `progettiMap` (riga 283) solo se `backlogSearch` valorizzato → digitando nella ricerca: `ReferenceError: Cannot access 'progettiMap' before initialization` → crash pagina | Spostare `progettiMap`/`risorsaMap` sopra `backlogTasks` | Basso | BUG REALE |
| G-06 | G | **P2** | Asse Y "Trend Margine" con dominio hardcoded `[0,60]` clippa margini >60% | Analytics.tsx:1414 | `<YAxis domain={[0,60]} />`. Margini >60% tagliati visivamente (schiacciati sul bordo). Dato a DB corretto, grafico fuorviante | `domain={[0,'auto']}` o `dataMax` con padding | Basso | BUG REALE |
| H-03 | H | **P2** | 426 errori eslint (`npm run lint` rosso) | frontend/ (es. types/index.ts:22,64,133; CaricoLavoroPage.tsx:97,100) | `npx eslint src` → **426 error + 20 warning**. Dominante `no-explicit-any`; alcuni `no-case-declarations`. Build/tsc passano (regola non bloccante) | Tipizzare gli `any` o declassare la regola a warning; wrappare i `case` in blocchi | Basso (config) / Medio (tipizzare) | BUG REALE (qualità) |
| H-04 | H | **P2** | Light mode ignorato da ~254 classi colore hardcoded | frontend/src (es. analytics/QuickCalculator.tsx:70,132; AIChatPanel.tsx:93,103) | `.light` esiste (index.css:54) + toggle (AppearanceSettings.tsx:51), ma 254 occorrenze `(bg|text|border)-[#hex]` + `text-white`/`bg-slate-900` senza `dark:`. In light mode contrasto rotto | Sostituire hex con token tema (`text-foreground`, `bg-card`, `text-muted-foreground`) | Medio (regressione visiva dark da verificare) | BUG REALE |
| H-05 | H | **P2** | N+1: query timesheet in loop nel calcolo affidabilità cliente | services.py:447-451 | `for c in commesse_anno:` esegue una `SELECT sum(durata_minuti) WHERE commessa_id=c.id` per commessa → N query | Aggregare con singolo `SELECT commessa_id, sum(...) GROUP BY commessa_id` | Basso | BUG REALE |
| H-06 | H | **P2** | N+1: lookup per-record nei loop di sync FiC | services.py:2225,2276,2353,2425 | Ogni `for raw in items:` fa `SELECT WHERE fic_id==...` per record (clienti/fornitori/fatture) → centinaia di query su sync grandi | Pre-caricare le entità per fic_id in un dict prima del loop | Medio (preservare logica upsert) | BUG REALE (mitigabile; sync manuale) |
| A-06 | A | **P3** | `DEVELOPER` non equiparato ad `ADMIN` in alcuni check espliciti | users.py:74,131; router.py:2822,2870,2897,2919 | DEVELOPER (ERP/finance pieno) non può patchare altri utenti né vede tutti i lead CRM; PM (deprecato) incluso in `is_manager`. Logica ruoli sparsa | Usare helper centralizzati (`has_erp_access`/`ADMIN_EQUIVALENT_ROLES`) invece di `== UserRole.ADMIN` | Basso | BUG REALE (minore) |
| A-07 | A | **P3** | Logout non revoca il JWT (stateless): token catturato valido fino a scadenza | auth.py:111-114 (`logout` fa solo `delete_cookie`) | Dopo `POST /auth/logout` (200), stesso token via `Authorization: Bearer` → `/auth/me` 200. Revoca reale solo via `DELETE /sessions`. Mitigato da httpOnly + TTL 60min | Accettabile per client cookie-only; per revoca immediata servirebbe denylist/sessioni server-side | — | ATTESO (architettura JWT stateless dichiarata) |
| B-05 | B | **P3** | `patch_cliente`: audit `emit_update` chiamato prima del check 404 | clienti.py:88-92 | Su id inesistente, `update_cliente` ritorna None ma `emit_update` scrive un record audit fittizio prima del 404 | Spostare il check `if not c: 404` prima di `emit_update` | Basso | BUG REALE |
| B-06 | B | **P3** | Doppio audit logging su update cliente | services.py:354 (`write_audit`) + clienti.py:89 (`emit_update`) | Ogni PATCH cliente scrive 2 righe audit; contribuisce al doppio flush di B-01 | Tenerne uno solo | Basso | BUG REALE |
| C-06 | C | **P3** | Badge commenti su kanban card mai visibile | StudioKanbanView.tsx:180 + useTasks.ts:27-50 | Il card legge `task.commenti_count` ma `TaskOut` non lo espone e `mapTask` non lo popola → badge sempre assente | Aggiungere `commenti_count` a `TaskOut` e mapparlo | Basso | BUG REALE |
| D-02 | D | **P3** | Service `list/create/update_risorsa` inline-only mai raggiunti via `/risorse` | services.py:3304,3316,3360 ca. | Usati solo dalle rotte shadowed (D-01); `costo_orario_effettivo` non arriva mai al client | Dopo D-01: spostare `costo_orario_effettivo` in RisorsaOut o eliminare i service inutilizzati | Basso | BUG REALE |
| D-03 | D | **P3** | Default `tipo_contratto` divergente tra creazione FE e seed | risorse.py:47 + model default | POST `/risorse` → `tipo_contratto:"DIPENDENTE"` (default model); seed usa `"Indeterminato"`. Campo non normalizzato | Normalizzare a enum/CHECK o documentare | Basso | BUG REALE |
| D-04 | D | **P3** | Possibile mismatch label `stato` Pianificazioni FE vs enum BE (da confermare FE) | pianificazioni.py:36 (enum `PENDING/ACCEPTED/CONVERTED`) | `GET /pianificazioni?stato=BOZZA` → 422. Se il FE invia label diverse il filtro fallisce; nessun valore hard-coded trovato nei type FE | Allineare costanti FE all'enum o mapping | Basso | DA CONFERMARE (FE) |
| E-01 | E | **P3** | quota Luca configurata (€1500/mese) ma sempre 0 in margine/P&L (dipende da `contenuti` vuota) | services.py:97-113,137-139 | `report/marginalita` → `quota_luca=0`; `count(contenuti)=0`; `SUM(quota_proforma_mensile)=1500`. `entry["totale"]==0` ⇒ 0 sempre | A regime: warning quando proforma>0 ma `totale==0` (quota "persa") | Basso (solo warning) | ATTESO (seed) |
| E-02 | E | **P3** | Pricing floor: `costo_manodopera` sempre 0 (risorse con `costo_orario_calcolato=0`) | pricing_floor.py:59-62 | POST `/pricing-floor/calcola` → `costo_manodopera:0.0` + warning. 12 risorse attive, 0 con costo>0 | Popolare `costo_orario_calcolato` nel seed; endpoint già avvisa | Nullo | ATTESO (seed) |
| E-03 | E | **P3** | Proiezione cassa: i 3 scenari danno curve identiche su tutti i 90gg | services.py:1428-1432 | Le 10 fatture aperte sono tutte scadute (mar–mag < inizio 2026-06-02) → clamp `idx=0` in ogni scenario. Con fatture future divergono | Nessun fix; comportamento corretto col seed | Nullo | ATTESO (seed) |
| E-04 | E | **P3** | DSO usa `data_ultimo_incasso` (non riconciliazione bancaria) | services.py:1142-1149 (TODO R2 presente) | DSO storico = `data_ultimo_incasso − data_emissione`. In attesa di FiC/riconciliazione attiva | Già tracciato come TODO(R2) | N/A | ATTESO (noto R2) |
| G-07 | G | **P3** | Refuso UI "Copertura Pipeline (Lavori Ceri)" | Analytics.tsx:1778 | Stringa visibile nelle card Forecast: "Lavori Ceri" (→ "Certi") | Correggere il testo | Nullo | BUG REALE |
| H-02 | H | **P3** | Intero modulo `routers/clickup.py` non incluso nell'app (codice morto) | routers/clickup.py:9 | `prefix="/api/v1/clickup"` ma nessun `include_router` lo referenzia → rotte irraggiungibili (root di H-01) | Montarlo (vedi H-01) o eliminarlo | Basso | BUG REALE |
| H-07 | H | **P3** | Modello/endpoint `Fattura` legacy potenzialmente non usati dal FE | router.py (Fattura legacy), models.py:570 | Coesistono `fatture`+`fattura_righe` (legacy) e `fatture_attive`/`_passive` (attuali, usate dal FE). Il FE chiama solo `fatture-attive/passive` | Verificare e deprecare il vecchio modello/endpoint se inutilizzato | Medio (dati storici) | DA VERIFICARE |

---

## Aree verificate OK (non-bug, per completezza)

- **Auth (A)**: cookie JWT httpOnly + secure(prod) + samesite=lax, TTL 60min; CORS whitelist con `allow_credentials`; token versioning (revoca via `DELETE /sessions`); bcrypt rounds=12; reset password token hashed/scadenza/single-use; tutti gli endpoint finance → 403 per non-finance, 200 per admin; `/uploads` anti path-traversal; rate limiting login/reset. FE `lib/access.ts` allineato al BE.
- **Core CRUD (B)**: Progetti e Commesse GET/POST/PATCH/DELETE OK (PATCH ri-fetcha correttamente); DELETE soft su clienti/progetti/commesse (niente violazioni FK); DELETE cliente con commesse aperte / fornitore con fatture → 400 corretto.
- **Studio OS (C)**: Timer start/stop crea/chiude `TimerSession` (verificato); pipeline contenuti endpoint OK (tabella vuota = ATTESO); `/studio/hierarchy`, `/timer/active*` → 200.
- **Team/HR (D)**: Timesheet GET/approva bulk OK, calcolo costi senza doppi conteggi (SUM completa su APPROVATO); Risorse CRUD soft-delete OK; DELETE `/risorse` duplicato già rimosso; Assenze/Planning/Collaboratori OK (vuoto seed = ATTESO).
- **Finanza (E)**: nessuna regressione; proiezione/marginalità/P&L/pricing coerenti; **quota Luca NON contata due volte**; `/report/marginalita` (R1) risolto.
- **Collaboration (F)**: Chat REST (canali/unread/mention/reazioni), Wiki (CRUD + `exclude_unset` + onupdate), Notifiche (dati reali, scope per utente, badge reale) tutti OK.
- **BI/Viz (G)**: Dashboard KPI = dati reali (cross-check `timesheet PENDING=12` FE==DB); Analytics charts bindati a dati reali (no mock); vuoto a giugno 2026 = ATTESO (seed fino ad apr-2026).
- **Trasversale (H)**: `npm run build` 0 errori; `tsc --noEmit` **0 errori TS**; **nessun drift modello↔DB**; migration custom (`schema_migrations` + 13 .sql) coerenti; nessun endpoint duplicato method+path oltre a D-01.
